= My Vim Config
This is my current vim configuration.

== Installation
This repository contains a `vimrc` file and a `vim` directory that should be linked to from your users directory as `.vimrc` and `.vim`.
	
	ln -nfs ~/VimSettings/vimrc .vimrc 
	
	ln -nfs ~/VimSettings/gvimrc .gvimrc

